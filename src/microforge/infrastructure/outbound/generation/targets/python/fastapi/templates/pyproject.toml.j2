[project]
name = {{ package_name | tojson }}
version = "0.1.0"
description = {{ description | tojson }}
requires-python = {{ requires_python | tojson }}
dependencies = [
  "fastapi>=0.115",
  "pydantic>=2.0",
  "uvicorn[standard]>=0.30",
  "sqlalchemy>=2.0",
]
